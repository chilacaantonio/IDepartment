<p>Bienvenido <%= @resource.name %> <%= @resource.paterno %> <%= @resource.materno %>!</p>

<p>Puedes confirmar tu cuenta a traves del siguiente enlace:</p>

<p><%= link_to 'Confirmar mi cuenta', confirmation_url(@resource, confirmation_token: @token) %></p>
